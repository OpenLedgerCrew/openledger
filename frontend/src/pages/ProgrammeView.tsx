export interface ProgrammeViewProps {
  programmeId: string;
}

/**
 * Section 6.1 — the public programme view: total disbursed, payment count, and delivery rate
 * visible without scrolling (item 2); the section 4.5 disclosure on the same screen, not below
 * the fold and not in a modal (item 3); the paginated payment table (item 4). No login (O-5).
 */
export function ProgrammeView(_props: ProgrammeViewProps) {
  return <main data-testid="not-implemented" />;
}
